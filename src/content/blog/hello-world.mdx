---
title: "블로그를 ���작하며"
description: "새로운 블로그의 첫 번째 글입니다. 이 블로그를 만든 이유와 앞으로의 계획에 대해 이야기합니다."
pubDate: 2026-04-04
tags: ["일상", "블로그"]
featured: true
---

## 시작

안녕하세요. 이상민입니다.

오랫동안 블로그를 시작해야겠다고 생각만 하다가, 드디어 첫 글을 쓰게 되었습니다. 연구를 하면서 배운 것들, 개발하며 겪은 시행착오, 그리고 기술에 대한 생각을 이곳에 기록하려 합니다.

## 왜 블로그를 만들었나

> 배운 것을 글로 정리하면, 비로소 자기 것이 된다.

연구 노트나 메모는 많이 남기지만, 다른 사람도 읽을 수 있는 형태로 정리하는 건 전혀 다른 일입니다. 글을 쓰는 과정에서 생각이 정리되고, 부족한 부분이 보이기도 합니다.

## 기술 스택

이 블로그는 다음 기술로 만들었습니다:

- **Astro** — 콘텐츠 중심의 정적 사이트 프레임워크
- **Tailwind CSS** — 유틸리티 기반 스타일링
- **MDX** — 마크다운 + 컴포넌트
- **GitHub Pages** — 무료 정적 호스팅

코드 블록도 잘 렌더링됩니다:

```python
import torch
import torch.nn as nn

class SimpleModel(nn.Module):
    def __init__(self, input_dim, hidden_dim, output_dim):
        super().__init__()
        self.layers = nn.Sequential(
            nn.Linear(input_dim, hidden_dim),
            nn.ReLU(),
            nn.Linear(hidden_dim, output_dim),
        )

    def forward(self, x):
        return self.layers(x)
```

## 앞으로

주로 다음 주제들에 대해 쓸 예정입니다:

1. 딥러닝 논문 리뷰
2. PyTorch 팁과 트릭
3. 연구 과정에서의 교훈
4. 개발 도구와 생산성

읽어주셔서 감사합니다. 좋은 글로 찾아뵙겠습니다.
