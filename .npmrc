# Fail npm install if Node version doesn't satisfy package.json engines.
# The ≥20.11 floor is load-bearing (import.meta.dirname in scripts/), so
# a silent install on older Node would break new-post and the audit.
engine-strict=true
